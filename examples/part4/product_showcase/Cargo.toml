[package]
name = "product_showcase"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
bevy = { workspace = true, features = ["file_watcher"] }

[[bin]]
name = "multi_camera_layers"
path = "src/bin/30d_multi_camera_layers.rs"
